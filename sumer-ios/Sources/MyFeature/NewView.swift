import SwiftUI

public struct NewView: View {

    public init() {}

    public var body: some View {
        VStack {
            Text("NewView!")
        }
        .padding()
    }
}

#Preview {
    NewView()
}
