import SwiftUI

public struct NewView: View {

    public init() {}

    public var body: some View {
        VStack {
            Text("NewView New Version Test Upload to app storessss!")
        }
        .padding()
    }
}

#Preview {
    NewView()
}
